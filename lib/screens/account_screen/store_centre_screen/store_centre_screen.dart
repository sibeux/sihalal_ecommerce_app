import 'package:flutter/material.dart';

class StoreCentreScreen extends StatelessWidget {
  const StoreCentreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Store Centre'),
      ),
    );
  }
}